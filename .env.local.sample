# Local Development Environment Configuration
# Copy this file to .env for local development

# Frontend and Backend URLs for local development
FRONTEND_URL=http://app.localhost:3000
BACKEND_URL=http://app.localhost:8000
BACKEND_PREFIX=api

# Base domain for multi-tenant subdomains (local development)
# The public tenant (main app) will be accessible at app.localhost:3000
# Individual tenants will be accessible at: demo.localhost:3000, client1.localhost:3000, etc.
BASE_DOMAIN=localhost

# Note: app.localhost serves as the public tenant/main application portal
# This follows the standard SaaS pattern where app.domain.com is the main application
# Individual tenant subdomains are created via the create_prod_tenant command