# Production Environment Configuration
# Copy this file to .env for production deployment

# Frontend and Backend URLs for production
# Replace 'yourdomain.com' with your actual domain
FRONTEND_URL=https://app.yourdomain.com
BACKEND_URL=https://app.yourdomain.com
BACKEND_PREFIX=api

# Base domain for multi-tenant subdomains (production)
# Replace 'yourdomain.com' with your actual domain
# The public tenant (main app) will be accessible at: app.yourdomain.com  
# Individual tenants will be accessible as: tenant1.yourdomain.com, tenant2.yourdomain.com, etc.
BASE_DOMAIN=yourdomain.com

# Production Notes:
# 1. Replace all instances of 'yourdomain.com' with your actual domain
# 2. Configure DNS records:
#    - A record: app.yourdomain.com → your-server-ip (public tenant/main app)
#    - A record: api.yourdomain.com → your-server-ip (API endpoint)  
#    - A record: *.yourdomain.com → your-server-ip (wildcard for all tenant subdomains)
# 3. Configure SSL certificates for wildcard domain: *.yourdomain.com
# 4. Main website yourdomain.com can serve marketing/static content (optional)
# 5. Use the create_prod_tenant command to create individual tenant subdomains:
#    docker-compose -f docker-compose-prod.yml exec backend python manage.py create_prod_tenant